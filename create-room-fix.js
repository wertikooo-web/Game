(()=>{function fieldValue(ids,fallbackSelector=''){for(const id of ids){const el=document.getElementById(id);if(el&&typeof el.value==='string')return el.value.trim()}const el=fallbackSelector?document.querySelector(fallbackSelector):null;return el&&typeof el.value==='string'?el.value.trim():''}window.createRoom=async function(){try{const name=fieldValue(['hostName','playerName','name'],'#app input[type="text"], #app input:not([type])');if(!name){const input=document.getElementById('hostName')||document.querySelector('#app input[type="text"], #app input:not([type])');if(input){input.focus();input.scrollIntoView({behavior:'smooth',block:'center'})}return}S.name=name;const mode=document.getElementById('mode'),level=document.getElementById('level'),rounds=document.getElementById('rounds');S.mode=mode?.value||S.mode||'solo';S.level=level?.value||S.level||'light';S.rounds=Number(rounds?.value||S.rounds||15);let expected=typeof getExpectedPlayers==='function'?getExpectedPlayers():Number(document.getElementById('expectedPlayers')?.value||2);if(S.mode==='couples'&&![4,6,8].includes(expected))return alert('Для режима «Пара против пары» выбери 2, 3 или 4 пары.');if(S.mode==='teams'&&expected<4)return alert('Для командной игры нужно минимум 4 игрока.');S.picked=questions();if(!S.picked.length)return alert('В этой колоде пока нет вопросов');const j=await api('create',{name:S.name,mode:S.mode,level:S.level,rounds:S.rounds,expected_players:expected});S.room=j.room;S.player=j.player;S.players=[j.player];S.host=true;S.screen='lobby';history.replaceState({},'',`${location.pathname}?room=${encodeURIComponent(S.room.code)}`);save();render();syncLoop()}catch(e){console.error('createRoom failed',e);alert(e?.message||'Не удалось создать комнату')}}})();